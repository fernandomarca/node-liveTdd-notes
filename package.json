{
  "name": "liveTdd",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev -r --inspect --transpile-only --ignore-watch node_modules --respawn src/server.ts",
    "test": "NODE_ENV=test jest --no-cache --detectOpenHandles",
    "test:watch": "NODE_ENV=test jest --passWithNoTests  --no-cache --watch --detectOpenHandles"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.12",
    "jest": "^27.4.4",
    "mockdate": "^3.0.5",
    "supertest": "^6.1.6",
    "ts-jest": "^27.1.1",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.3"
  }
}
